apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: mtls-mode
  namespace: default
spec:
  mtls:
    mode: STRICT

# once you apply this configuration, only the services that have the certificate can access the service in the default namespace.